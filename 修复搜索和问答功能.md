# 修复搜索和问答功能

## 问题诊断

### 问题1: 搜索返回 0 结果 ❌

**后端日志**:
```
Search: '戴墨镜海边拍照的照片' -> 0 results
```

**根本原因**: `SearchService` 中 `user_id` 类型不匹配
- `self.user_id` 是字符串类型（如 `"26f7fed3-..."`）
- `Content.user_id` 是 UUID 类型
- SQLAlchemy 直接比较字符串和 UUID 会导致查询失败

**代码位置**: `backend/app/services/search_service.py` 第113行

### 问题2: 搜索API无限重定向 ❌

**前端日志**:
```
GET /api/search/?q=... 307 Temporary Redirect (循环)
Removed trailing slash from path: /api/search/ -> /api/search
```

**根本原因**: 代理中间件和 FastAPI 自动重定向冲突

**代码位置**: `backend/app/main.py` 第82-90行

### 问题3: QA无法找到相关内容 ❌

**表现**: "抱歉，我在知识库中没有找到相关信息来回答您的问题。"

**根本原因**: 搜索返回 0 结果 → QA 无法构建上下文

---

## 修复内容

### ✅ 修复1: user_id 类型转换

**文件**: `backend/app/services/search_service.py`

**修改前**:
```python
if self.user_id:
    base_query = base_query.filter(Content.user_id == self.user_id)
```

**修改后**:
```python
if self.user_id:
    from uuid import UUID
    user_uuid = UUID(self.user_id) if isinstance(self.user_id, str) else self.user_id
    logger.info(f"🔍 Filtering by user_id: {self.user_id} (UUID: {user_uuid})")
    base_query = base_query.filter(Content.user_id == user_uuid)
```

**修复位置**:
- 第112-118行（关键词搜索）
- 第740-744行（分类统计）

### ✅ 修复2: 搜索API无限重定向

**文件**: `backend/app/main.py`

**修改**:
- 移除了通用的尾随斜杠处理
- 添加了针对搜索API的特殊处理

### ✅ 修复3: 添加用户认证到搜索API

**文件**: `backend/app/api/search.py`

**修改**:
- 为 `search_by_category` 添加 `current_user` 认证 ✅
- 为 `get_search_suggestions` 添加 `current_user` 认证 ✅
- 传递 `user_id` 给 `SearchService` ✅

### ✅ 修复4: 添加详细日志

**文件**: `backend/app/services/search_service.py`

**添加了调试日志**:
```python
logger.info(f"🔍 Filtering by user_id: {self.user_id} (UUID: {user_uuid})")
logger.info(f"📊 Keyword search found {len(results)} results after exact match")
logger.info(f"📊 Keyword search found {len(results)} results after word match")
logger.info(f"✅ Keyword search completed with {len(results)} final results")
```

---

## 验证修复

### 需要先执行数据库迁移

⚠️ **重要**: 必须先执行 SQL 迁移修复 NULL 的 user_id

```bash
# 在云端服务器执行
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb << 'EOF'
UPDATE contents SET user_id = (SELECT id FROM users WHERE email = 'test@pkb.local' LIMIT 1) WHERE user_id IS NULL;
UPDATE collections SET user_id = (SELECT id FROM users WHERE email = 'test@pkb.local' LIMIT 1) WHERE user_id IS NULL;
ALTER TABLE contents ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE collections ALTER COLUMN user_id SET NOT NULL;
EOF
```

### 部署新代码

```bash
# 1. 拉取代码
cd /path/to/pkb-poc
git pull

# 2. 重启服务
docker compose -f docker-compose.cloud.yml restart pkb-backend pkb-celery

# 3. 查看日志
docker compose -f docker-compose.cloud.yml logs -f pkb-backend | grep -i "search\|qa"
```

### 测试验证

1. **测试搜索**:
   - 搜索 "风力发电机" → 应该返回相关结果
   - 搜索 "海边" → 应该返回海边相关的图片

2. **测试QA**:
   - 提问 "在海边拍照的照片" → 应该返回相关照片信息
   - 提问 "戴墨镜海边拍照的照片" → 应该返回匹配的照片

3. **检查日志**:
   ```bash
   docker compose -f docker-compose.cloud.yml logs pkb-backend | grep "🔍\|📊\|✅"
   ```

**期望看到**:
```
🔍 Filtering by user_id: 26f7fed3-... (UUID: ...)
📊 Keyword search found 5 results after exact match
✅ Keyword search completed with 5 final results
```

---

## 修改的文件汇总

1. ✅ `backend/app/services/search_service.py` - 修复 user_id 类型
2. ✅ `backend/app/services/category_service.py` - 修复 content_uuid
3. ✅ `backend/app/api/search.py` - 添加用户认证
4. ✅ `backend/app/main.py` - 修复无限重定向
5. ✅ `backend/app/models.py` - user_id 改为 NOT NULL
6. ✅ `backend/app/api/files.py` - Google Drive 缩略图支持
7. ✅ `frontend/src/components/Document/*.tsx` - Google Drive 支持

---

## 下一步

1. ✅ 执行 SQL 迁移（修复 NULL user_id）
2. ⏳ 推送代码到云端
3. ⏳ 重启服务
4. ⏳ 验证搜索和 QA 功能

**部署顺序**：
```bash
# 先执行SQL迁移
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "UPDATE contents SET user_id = (SELECT id FROM users WHERE email = 'test@pkb.local' LIMIT 1) WHERE user_id IS NULL;"
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "ALTER TABLE contents ALTER COLUMN user_id SET NOT NULL;"

# 再部署代码
cd /path/to/pkb-poc && git pull
docker compose -f docker-compose.cloud.yml restart pkb-backend pkb-celery
```

---

## 如果还有问题

### 问题A: 搜索仍然返回 0 结果

**可能原因**:
1. 数据库中的 content 记录没有正确的 user_id
2. 图片没有进行 OCR，text 字段为空
3. 搜索查询太具体，无法匹配

**检查**:
```bash
# 检查数据库中的 user_id
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "
SELECT id, title, user_id, LENGTH(text) as text_length 
FROM contents 
ORDER BY created_at DESC 
LIMIT 10;
"
```

### 问题B: QA 仍然返回 "没有找到相关信息"

**可能原因**:
- 搜索返回 0 结果（参考问题A）
- QA 服务的搜索过滤器太严格

**解决**: 确保先修复搜索问题

