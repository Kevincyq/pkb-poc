# 搜索和问答逻辑检查报告

## ✅ 已确认正确的逻辑

### 1. 用户隔离逻辑
- ✅ **搜索服务**: 正确传递 `user_id` 到 `SearchService`
- ✅ **关键词搜索**: 使用 UUID 转换过滤 `Content.user_id`
- ✅ **语义搜索**: 使用 CAST 转换 UUID 类型
- ✅ **QA 服务**: 正确传递 `user_id` 到 `QAService` → `SearchService`
- ✅ **QA 历史**: 保存时包含 `user_id`，查询时过滤

### 2. 搜索流程
```
用户提问 → QA API (/qa/ask)
  ↓
QAService.ask()
  ↓
SearchService.search()  [传递 user_id]
  ↓
混合搜索 (hybrid) 或 纯语义/关键词
  ↓
关键词搜索: base_query.filter(Content.user_id == user_uuid)
  ↓
语义搜索: WHERE contents.user_id = CAST(:user_id AS UUID)
  ↓
返回格式化的结果
```

### 3. QA 流程
```
SearchService.search() → 获取相关文档
  ↓
_build_context() → 构建上下文（限制字符数）
  ↓
_filter_high_quality_sources() → 过滤高质量来源
  ↓
_generate_answer() → 调用 Turing API 生成回答
  ↓
_save_qa_history() → 保存问答历史（包含 user_id）
```

---

## ⚠️ 发现的问题

### 问题1: 语义搜索的 SQL 参数绑定

**位置**: `backend/app/services/search_service.py` 第264行

**问题**: 使用了 `CAST(:user_id AS UUID)`，但 SQLAlchemy 的参数绑定可能有兼容性问题

**修复**: 
- 改为使用 `.bindparams()` 明确绑定参数
- 添加错误处理和日志

### 问题2: 搜索结果为空的可能原因

**症状**: `Keyword search found 0 results`

**可能原因**:
1. 数据库中该用户确实没有内容
2. 搜索词与内容不匹配
3. 图片没有 OCR 文本
4. chunks 表为空或没有对应 content

---

## 🔍 诊断建议

### 检查1: 数据库中有没有数据
```bash
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "
SELECT 
    COUNT(*) as total_contents,
    COUNT(DISTINCT c.id) FILTER (WHERE c.user_id = '26f7fed3-939c-4bb6-afb5-a1a2ab1934e1'::uuid) as user_contents,
    (SELECT COUNT(*) FROM chunks WHERE content_id IN 
        (SELECT id FROM contents WHERE user_id = '26f7fed3-939c-4bb6-afb5-a1a2ab1934e1'::uuid)
    ) as user_chunks
FROM contents c;
"
```

### 检查2: 测试搜索参数
```bash
# 检查是否有 chunks
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "
SELECT 
    c.title,
    c.user_id,
    LENGTH(ch.text) as chunk_length
FROM chunks ch
JOIN contents c ON ch.content_id = c.id
WHERE c.user_id = '26f7fed3-939c-4bb6-afb5-a1a2ab1934e1'::uuid
LIMIT 5;
"
```

### 检查3: 测试关键词搜索
```bash
# 直接测试搜索
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "
SELECT 
    c.title,
    ch.text,
    c.user_id
FROM chunks ch
JOIN contents c ON ch.content_id = c.id
WHERE c.user_id = '26f7fed3-939c-4bb6-afb5-a1a2ab1934e1'::uuid
  AND (ch.text LIKE '%海边%' OR c.title LIKE '%海边%')
LIMIT 5;
"
```

---

## 📋 代码逻辑总结

### 搜索服务 (`SearchService`)

**初始化** (第19-26行):
```python
def __init__(self, db: Session, user_id: Optional[str] = None):
    self.user_id = user_id
    # 创建 UserContextService
```

**关键词搜索** (第95-201行):
```python
# 1. 构建基础查询
base_query = db.query(...).join(Content).outerjoin(Category)

# 2. 用户隔离
if self.user_id:
    user_uuid = UUID(self.user_id)
    base_query = base_query.filter(Content.user_id == user_uuid)

# 3. 搜索匹配
conditions = [text("chunks.text LIKE :query"), ...]
results = base_query.filter(or_(*conditions)).params(**params)
```

**语义搜索** (第203-308行):
```python
# 1. 获取查询向量
query_embedding = embedding_service.get_embedding(query)

# 2. 构建原生 SQL
base_sql = f"""SELECT ... FROM chunks JOIN contents ..."""

# 3. 用户隔离
if self.user_id:
    where_conditions.append("contents.user_id = CAST(:user_id AS UUID)")
    params['user_id'] = str(self.user_id)

# 4. 执行查询
bound_sql = sql.bindparams(**params)
results = db.execute(bound_sql).fetchall()
```

### QA 服务 (`QAService`)

**初始化** (第25-60行):
```python
def __init__(self, db: Session, user_id: Optional[str] = None):
    self.user_id = user_id
    self.search_service = SearchService(db, user_id)  # ✅ 传递用户ID
```

**提问** (第99-220行):
```python
def ask(...):
    # 1. 搜索相关文档
    search_results = self.search_service.search(
        query=question, 
        top_k=5, 
        search_type=search_type,
        filters=filters
    )
    
    # 2. 构建上下文
    context, sources = _build_context(search_results["results"], context_limit)
    
    # 3. 生成回答
    answer_data = _generate_answer(...)
    
    # 4. 保存历史
    qa_record = _save_qa_history(
        user_id=self.user_id,  # ✅ 用户隔离
        ...
    )
```

---

## 🔧 最新修复

### 修复1: SQL 参数绑定 (第293-317行)
```python
# 添加了详细的日志和错误处理
if params:
    bound_sql = sql.bindparams(**params)
    results = self.db.execute(bound_sql).fetchall()
    logger.info(f"📊 Semantic search executed with params: {list(params.keys())}")
    
logger.info(f"📊 Semantic search found {len(results)} results")
```

### 修复2: 用户ID类型转换
```python
# 关键词搜索
user_uuid = UUID(self.user_id) if isinstance(self.user_id, str) else self.user_id
base_query = base_query.filter(Content.user_id == user_uuid)

# 语义搜索
where_conditions.append("contents.user_id = CAST(:user_id AS UUID)")
params['user_id'] = str(self.user_id)
```

---

## ✅ 验证清单

1. ✅ 用户隔离逻辑正确实现
2. ✅ SQL 参数正确绑定
3. ✅ UUID 类型转换正确
4. ✅ 错误处理和日志完善
5. ✅ QA 服务调用搜索服务时传递 user_id
6. ✅ QA 历史保存时包含 user_id

---

## 🎯 下一步

1. **部署修复后的代码**
2. **检查数据库** - 确认有数据
3. **测试搜索** - 使用调试日志查看结果
4. **测试 QA** - 验证问答功能

---

## 📝 部署命令

```bash
# 1. 拉取代码
cd /path/to/pkb-poc && git pull

# 2. 重启服务
docker compose -f docker-compose.cloud.yml restart pkb-backend

# 3. 查看日志
docker compose -f docker-compose.cloud.yml logs -f pkb-backend | grep -E "🔍|📊|✅|Search:"
```

---

## 🔍 如果仍然返回 0 结果

执行诊断查询：
```bash
# 检查数据库中是否有该用户的内容和 chunks
docker compose -f docker-compose.cloud.yml exec postgres psql -U pkb -d pkb -c "
SELECT 
    c.id,
    c.title,
    c.user_id,
    COUNT(ch.id) as chunk_count,
    SUM(LENGTH(ch.text)) as total_text_length
FROM contents c
LEFT JOIN chunks ch ON ch.content_id = c.id
WHERE c.user_id = '26f7fed3-939c-4bb6-afb5-a1a2ab1934e1'::uuid
GROUP BY c.id, c.title, c.user_id
ORDER BY c.created_at DESC
LIMIT 10;
"
```

**如果没有数据**: 需要先上传文件
**如果有数据但没有 chunks**: 需要重新处理文件
**如果有 chunks**: 检查搜索词是否匹配

