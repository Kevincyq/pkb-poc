# 修复QA返回重复文档问题

## 🔍 问题分析

### 症状
QA 返回的相关文档中存在重复文件（如 `fuzhou1.jpg` 出现两次），且相关度不一致。

### 根本原因
1. **搜索阶段**：同一个文档（content）可能有多个 chunks，每个 chunk 都有不同的相关度
2. **去重不完整**：虽然 `_format_semantic_results` 有去重逻辑，但可能在某些边界情况下失效
3. **QA 阶段**：`_build_context` 方法没有进行文档级别的去重，导致重复的文档都被添加到 sources

### 后端日志显示
```
📊 Semantic search found 10 results
  Result 1: fuzhou3.jpg (distance: 0.683157205581665)
  Result 2: fuzhou3.jpg (distance: 0.683157205581665)  # 重复
  Result 3: fuzhou1.jpg (distance: 0.7396868467330933)
...
Original sources count: 5
Filtered sources count: 4
Source 2: fuzhou1.jpg - 67.0%
Source 3: fuzhou1.jpg - 59.0%  # 重复！
```

---

## ✅ 修复方案

### 修复1: 增强语义搜索结果的去重日志

**文件**: `backend/app/services/search_service.py`

**修改**: 在 `_format_semantic_results` 方法中添加详细的去重日志

**效果**:
- 记录去重过程
- 输出去重后的文档数量
- 显示跳过的重复 chunks

### 修复2: QA 上下文构建时的文档去重

**文件**: `backend/app/services/qa_service.py`

**修改**: 在 `_build_context` 方法中添加文档级去重

**关键代码**:
```python
seen_content_ids = {}  # 用于去重，跟踪已添加的文档

for result in search_results:
    content_id = result["content_id"]
    
    # 去重：如果这个文档已经添加过，跳过
    if content_id in seen_content_ids:
        logger.debug(f"Skipping duplicate content: {result['title']} (ID: {content_id})")
        continue
    
    # 标记该文档已被添加
    seen_content_ids[content_id] = True
    # ... 添加到 context 和 sources ...
```

### 修复3: 搜索结果合并时的日志

**文件**: `backend/app/services/search_service.py`

**修改**: 在 `_merge_search_results` 方法中添加详细日志

**效果**:
- 记录合并过程
- 显示最终唯一的文档数量
- 打印前3个合并后的结果

---

## 📋 修改的文件

1. ✅ `backend/app/services/search_service.py` - 增强去重逻辑和日志
2. ✅ `backend/app/services/qa_service.py` - 添加文档级去重

---

## 🚀 部署步骤

```bash
# 1. 拉取代码
cd /path/to/pkb-poc && git pull

# 2. 重启服务
docker compose -f docker-compose.cloud.yml restart pkb-backend

# 3. 查看日志
docker compose -f docker-compose.cloud.yml logs -f pkb-backend | grep -E "📋|✅|🔀|📚|Skipping duplicate"
```

---

## ✅ 验证

### 测试问答 "风力发电机"

**预期**:
- 不再出现重复的 `fuzhou1.jpg`
- 每个文档只出现一次
- 日志中显示 "Skipped X duplicate chunks"

**查看日志**:
```bash
docker compose -f docker-compose.cloud.yml logs pkb-backend | grep -A 5 "Formatting semantic results"
```

**期望输出**:
```
📋 Formatting semantic results: 10 chunks, threshold=0.350
✅ Formatted 5 unique documents (skipped 5 duplicate chunks)
🔀 Merging results: keyword=0, semantic=5
🔀 Merged results: 5 unique documents
📚 Built context from 5 unique documents (skipped 0 duplicates)
```

---

## 🔍 如果仍然有重复

检查日志中的详细调试信息：
```bash
# 查看搜索过程的去重日志
docker compose -f docker-compose.cloud.yml logs pkb-backend | grep -E "New document|Skipping.*duplicate|Replacing chunk"
```

**期望看到**:
- `New document: fuzhou3.jpg (sim=0.317)`
- `Skipping lower score chunk for fuzhou1.jpg: 0.410 <= 0.426`
- `Replacing chunk for fuzhou1.jpg: 0.410 -> 0.426`

这表示去重逻辑正在工作。

